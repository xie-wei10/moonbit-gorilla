# 功能与兼容性边界

0.3：真正逐条追加的编码器、独立快照、封闭生命周期、常量状态解码迭代器、严格尾部验证，
批量接口复用同一核心。写入器按字节打包；范围查询仅保留命中样本。提供本地 GOR1 文件 CLI。

保持既有自定义 GOR1 格式，不承诺兼容其他 Gorilla/TSDB 位流。
仍缺独立对照、真实数据性能测量、索引/随机访问、部分字节输入、校验和与参考格式模式。

本轮 13 项 JS 测试和 5 种文件 CLI 场景通过，包括 100,000 样本与原始浮点位模式。
不据此宣称全面追平；没有上传、发布或重复刷新历史压缩包。
