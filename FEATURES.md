# 兼容矩阵 · 0.4.0

| 能力 | 当前证据与边界 |
|---|---|
| GOR1 编码、快照、逐条解码、范围查询 | 原格式保留；144 项独立 Python 规格模型字节/样本对照 |
| Prometheus 原始 XOR chunk | 277 项未修改 v0.314.0 官方库精确字节/双向对照；38 固定向量跨后端；支持完整 Int64 与原始 UInt64 值 |
| 带 CRC 的分块索引容器 | 原创 GOR2；Python struct/zlib + 官方负载 codec 的 5 个独立容器对照 |
| 增量字节输入与真实流式文件 | GOR2 任意分片、最终索引验证；10 万样本及错误/取消/关闭测试；旧块格式仍接收完整块 |
| 索引范围查询 | 二分定位；窄查询实测读取不足文件 10%；默认只验证选中负载，verifyAll 检查全部 |
| 数据与性能 | 3 条观测序列（NOAA/USGS）和 3 条合成序列；字节与参考一致，JS 速度仍有明显差距 |
| 原论文/Beringei、完整 TSDB segment/block/WAL、多序列管理 | 尚未完成，不用 Prometheus chunk 兼容替代这些目标 |
| 生产负载、跨平台、长期故障、内存/吞吐成熟度 | 尚未完成；单机测试和小规模观测数据不是全面证明 |

保留全部追平要求，未缩为通过现有用例。仓库只在本地，无上传/发布；旧归档仍为历史快照。
